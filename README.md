# ept

```
ept

ARGS:
    <path>
      Path to book.

OPTIONS:
    -w, --width <width>
      Width of text view, in characters.
      Defaults to 60.

    -h, --help
      Prints help information.
```
